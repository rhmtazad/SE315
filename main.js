// changes background color of an element
function changeElementColor(element, color) {
  element.style.color = color;
}

// gives an alert and says hello
function sayHello(message, element, color) {
  alert("Hello" + message);
  element.style.color = color;
}

// gives an alert that website is under constraction
function submissionAlert(message) {
  alert("Oops!\nWebsite under constraction...");
}
